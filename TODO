SRJS.Vision.detectBlobs()
 - Can create large numbers of span objects per frame. Possibly cache or reuse.

SRJS.Vision.prototype.rgbToHsv()
 - ~30% of time when profiling. Could do with optimisation.

Physics
 - Collision detection
